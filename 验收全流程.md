# 验收全流程

## 再建库
```bash
cmake --build build
```

## 启动
```bash
build\mrag.exe
```

## 库检查
```bash
dir data\*.db
```

## 多文档建库演示 `--ingest`

```bash
build\mrag.exe --ingest data\test_sanguo_50.txt data\test_honglou_50.txt data\test_multi_50.db
```
```
metadata 中加入了源文件名，所以后续检索能看出片段来自三国还是红楼。
```

## 启动 `--chat`

```bash
build\mrag.exe --chat data\sanguo.db
```

*示例问题*
```text
刘备的字是什么？
```

```text
关羽的武器是什么？
```

## 单次查询 & 多文档检索演示

```bash
build\mrag.exe --query data\test_multi_50.db "通灵宝玉是谁带到人间的？"
```

## 配置文件和模型热切换演示

```bash
build\mrag.exe --chat data\test_multi_50.db
```

```bash
/reload
```

```json
"generation": "./models/qwen2.5-1.5b-instruct-q4_k_m.gguf"
```

## 模型演示命令

```bash
build\mrag.exe --probe-emb models\bge-small-zh-v1.5-f16.gguf
build\mrag.exe --probe-gen models\qwen2.5-1.5b-instruct-q4_k_m.gguf
build\mrag.exe --probe-gen models\qwen2.5-7b-instruct-q4_k_m.gguf
```

## 总结

```text
项目分离线建库和在线问答两阶段。离线建库读取 TXT，按章节和 UTF-8 安全边界切 Chunk，用 Embedding 模型生成向量，并写入二进制 .db。在线问答加载 .db，把用户问题转成向量，用混合检索召回 top-k 片段，并打印来源和预览，再把片段放入 prompt 交给本地 GGUF 模型生成回答。项目支持多文档建库、交互式 /reload 热切换模型、单次 query、模型探测和 Valgrind 检查。
```